# JDI RPA Mini Project – Request Processing Automation

A Python-based RPA-style automation that classifies and validates internal requests, generates an Excel report (KPIs, Issues, Summary), and logs exceptions.

## Run
pip install -r requirements.txt
python src/main.py

## Output
- outputs/output_report.xlsx (Processed, Issues, CategorySummary, KPIs)
- outputs/logs.txt
